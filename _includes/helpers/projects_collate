{% for project in site.projects %}
	<h2><a class="active internal" href="{{ project.url | relative_url }}">{{ project.title }}</a></h2>
	<p>{{ project.description }}</p>
{% endfor %}

{% assign projects_collate = nil %}
